# 工作包：A01

状态：review
负责人：opencode（待集成负责人评审）
开始基线：`c6f0c1d feat: add procedural music, game feel systems and exploration content`；工作区另含未提交的 art-overhaul 规划文档与 README 链接。
依赖：无（M0 入口包）。
范围：visual_qa 基础场景 / 基线元数据；详见 00 的 0.3/0.4、05 的 5.2 A01、06 的 6.1/6.8。

## 交付物

- `src/visual_qa/mod.rs`：`--visual-qa` 工具入口、配置解析、运行状态机、相机/时钟锁定、截图、帧时统计、manifest/metrics/summary 报告。
- `src/visual_qa/scene.rs`：确定性 S01 材料庭院、S02 封闭屋（有窗/开门/拆墙/无窗暗室）、S06 地表流式路线。
- `src/visual_qa/baseline.rs`：基线 HEAD、`src` 物理行数、资产清单/hash/忽略大素材、硬件与系统内存快照。
- `src/main.rs`：+22 行接入（模块声明、CLI 解析、内存内禁用像素模式、`WorldRequest`、插件装配）。
- 本文档与 `task-index.json` 状态更新；不改存档、网络协议、业务逻辑与依赖。

## 入口、默认值与退出码（真实实现，非规划草案）

```powershell
cargo run --locked -- --visual-qa                                  # S01+S02+S06，seed=11，lush，day_time=0.5，route=900 帧
cargo run --locked -- --visual-qa --visual-qa-scene S01            # 单场景
cargo run --locked -- --visual-qa --visual-qa-scene S01,S06        # 场景列表
cargo run --locked -- --visual-qa --visual-qa-seed 1337 --visual-qa-biome frozen --visual-qa-day 0.75
cargo run --locked -- --visual-qa --visual-qa-out target/visual-qa --visual-qa-run my-run
cargo run --locked -- --visual-qa --visual-qa-route-frames 240
cargo run --locked -- --visual-qa --visual-qa-no-capture --visual-qa-keep-open
```

- 报告路径：`<out>/<commit7>/<scene>/<run>/manifest.json`、`metrics.json`、`raw-frames/*.png`；整轮摘要 `<out>/<commit7>/<run>-summary.json`。默认 `<out>=target/visual-qa`。
- 退出码：`0` 全部场景完成；`2` 任一场景/资源失败（仍写 summary）；不写用户 `saves/` 与 `settings.json`。
- 未知参数与非法值保留默认并打印警告；`--visual-qa` 只在内存中强制 `settings.pixelated=false`，不落盘。

## 实施子步骤（完成情况）

1. 基线记录：HEAD、`src` 48 文件/51,561 物理行、191 个资产/729,081,589 字节/hash `30b79675187eee25`、`models/external`、`models/earth`、`../models` 的 present/missing、忽略文件清单、GPU/驱动/窗口 DPI/系统内存。✅
2. S01：混凝土 25×25 平台，4 行共 28 种代表方块（土石/木/玻璃/金属/矿石/发光/低盒/水生植物）、金属体素球、树、板阶、水坑；4 个固定机位。✅
3. S02：9×9 混凝土房间（金属角柱、开门、玻璃窗、2×2 破损墙、顶灯）+ 5×5 全封闭金属暗室；4 个固定机位。✅
4. S06：13 个沿 +X 的地形高度航点，进度参数化相机，10%/50%/90% 抓图；900 帧默认。✅
5. manifest + 硬件参数 + 资产快照；CPU 帧时 p50/p95/p99/mean/max、实体/网格/图像计数写入 metrics。✅
6. 复现验证：同参数两次运行 (`S01/a01-det-c` vs `S01/a01-det-d`) 的 `manifest.json` SHA-256 完全相同（`MANIFEST_IDENTICAL`）。✅
7. 用户存档保护：运行前后 `saves/` 三个文件时间戳保持 02:06:51（本次 QA 运行为 05:00+），未写入。✅

## 验收场景（本轮实测）

- 运行 `a01-review`：`VISUAL_QA_OK scenes=3`，commit `c6f0c1d`，seed 11，lush，day_time 0.5，route 900。
- 证据：`target/visual-qa/c6f0c1d/a01-review-summary.json` 及 `S01|S02|S06/a01-review/{manifest,metrics}.json`、`raw-frames/*.png`（target 不入库；用上面命令可复现）。
- 调试构建 CPU 帧时（非发布验收数字，仅证明采集链路）：S01 p50 14.3ms / p95 46.5ms，S02 p50 11.4 / p95 39.4，S06 p50 12.8 / p95 17.3 / p99 28.6（1056 帧）。

## 命令与结果

```powershell
cargo fmt --all -- --check
cargo check --locked --all-targets --all-features
cargo clippy --locked --all-targets --all-features -- -D warnings
cargo test --locked --all-targets --all-features
# 全部通过：231 passed / 0 failed（新增 10 个 visual_qa 单测）
```

## 兼容、回滚与故障注入

- 兼容影响：新增 CLI 分支与模块；不改变存档 schema、RNG 语义、网络协议、依赖。默认不启用，正常启动路径行为不变。
- 回滚：删除 `src/visual_qa/`、`main.rs` 的 4 处接入和 `task-index.json` 状态即可；无迁移。
- 已验证故障：非法 seed/day/route-frames/biome/scene 回退默认（单测）；percentile 空集与顺序（单测）；`git HEAD` 分离/引用两种形态与仓库在 crate 上层（单测）；route 端点/中点（单测）；`--visual-qa` 未开启时插件为 no-op。
- 实施中发现并修复：S06 路线抓图循环未推进 `captured`，导致 Screenshot 命令无限入队（24GiB 分配失败，exit 0xc0000409）。修复为按进度触发并计数；此后 240/900 帧两种路线均通过。该 bug 只存在于本轮新增代码，未进入基线。
- 未验证：缺 `World`/相机资源的失败路径未做真实注入（仅代码分支存在）。

## Rust 净增（物理行，2026-09-12 统计）

| 类别 | 新增 | 删除 | 说明 |
| --- | ---: | ---: | --- |
| 工具代码（visual_qa 非测试） | 1,715 | 0 | baseline 301 + mod 941 + scene 473 |
| 测试（`#[cfg(test)]`） | 160 | 0 | baseline 39 + mod 74 + scene 47 |
| 运行时接入（main.rs） | 22 | 0 | 模块/CLI/插件装配 |
| **合计** | **1,897** | **0** | A01 预算 700，超出 +171% |

超预算说明（按 05 的 5.13 规则）：A01 是本轮从零搭建的端到端工具，包含真实场景几何、manifest schema、硬件/资产基线和报告写入，未使用占位或填充；没有可安全削减的兼容/QA 项。若需守住流 A 总量，建议从 A02–A06 的规划分配中抵扣，或按评审意见拆分本包（例如把 S06 拆为 A01b）。

## 未完成 / 未验证（必须由后续包接手）

1. 人工视觉评审与 1–5 分锚点打分：K05；本轮只提供原始图，未做“前后对照”。
2. GPU pass 级耗时、显存与进程 RSS/GPU 常驻：未测；metrics 只含主线程 CPU 帧时。GPU timestamp 能力按 D01 采。
3. 云/天气相位确定性：`weather::ClimateRuntime.elapsed` 目前私有且随时间推进；manifest 记录 day_time 与抓图时的 harness 云龄估计，E02/E03 需暴露显式云时钟，否则 S08/S09 无法严格复现。
4. `visual_style_version=1` 与 `generator_version=commit` 是临时占位，等 B/G06 定义真实版本字段；冻结后需更新 schema（按 ADR）。
5. 观察到的基线现象（供 C/F/D 复核，不计为已确认 bug）：
   - S01 `samples` 机位在平台边缘可见整幅偏黄绿半透明覆盖，疑似远地形 far mesh 挖空 alpha 与玩家快速传送/hole 节流不同步或与近网格叠加；对照 S02/S06 正常帧。复测建议：C06/S05。
   - S06 相机按地形高度飞行，会穿过树冠，个别帧出现整屏叶色（`route_018`）；F/LOD 可决定是否清路或提高航线。
   - 兽群/NPC/HUD/天气粒子未暂停，抓图含实时玩法元素；布局与坐标确定，但生物位置随帧率变化。后续如需纯静态对照，可在 visual_qa 增加“冻结玩法”开关（不属 A01 验收）。
6. 缺 `--visual-qa` 与联机/读档/换星并用的组合测试；S21/S22 归 K02。

## 下一包建议

A02（`VisualFrame` 等公共数据契约）在 A01 的 manifest 字段稳定后开始；建议把本包的 3 个机位/scene_id 作为 A02 的首批消费者，并把 `world_epoch` 与 `frame_id` 加入 manifest 的后续 schema 版本。
