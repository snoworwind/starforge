# 工作包：B01

状态：review
负责人：opencode（待集成负责人评审）
开始基线：`c6f0c1d`；工作区含 A01–A06 未提交实现与规划文档。
依赖：A01（确定性场景/报告运行器，review）。
范围：`art/manifest`、风格/尺度样板；详见 05 的 5.3 B01、01 的 1.1/1.2/1.5、06 的 R013–R024 与 6.8。

## 交付内容

1. `src/art/manifest.rs`：资产清单 + 文件系统审计
   - `builtin_manifest()` 从代码常量反查，不读 README：`char::NPC_MODELS`（8）、`creatures::creature_model`（6 种 → 4 个 Quaternius 文件）、`sentinel`、陨石、外部船/站/地球、Kenney reserve 船/访客、Poly Pizza 遗留生物、3 个 WGSL。共 39 条：18 live / 11 reserve / 10 optional_download。
   - 每条含稳定 key、类别、运行时路径、来源/许可/许可文件、角色、`style_version`、用途备注；`scene.gltf` 用父目录名做 key，避免外部模型全部撞成 `ship.scene`。
   - `audit(root, source_tree)` 检查：必发资产缺失、**逐段路径大小写**（Windows 也检出）、许可证文件缺失、`models/`+`shaders/` 下未被引用的 `.glb/.gltf/.wgsl`；`run_audit` 写 `target/art-audit/art-manifest.json` 并按 `ART_AUDIT_OK/FAIL` 返回退出码（硬失败=缺失/大小写）。
2. `src/art/style.rs`：`ART_STYLE_VERSION=1`、6 色全局基础色板、3 级发光档、7 条尺度锚点（方块/眼高/人形身高/门净空/墙高/船体包络/碰撞半径），全部标注来源符号。
3. `--art-audit` CLI（接入 A06 的 `LaunchOptions`）：不建 App、不开窗、不碰存档/GPU，审计后直接退出。
4. B01 视觉校准架（`visual_qa`）
   - `SceneId::B01`：地形（草/土/沙/石/玄武岩/雪/冰）、机器（熔炉/矿机/装配机/反应堆/太阳能/电池，含 `factory::spawn_machine` 实体）、角色（KayKit 骑士 + Kenney 宇航员 + 外星人，`spawn_humanoid_model` 固定队列）、建筑（7×7 原木角柱 + 木板墙/顶 + 2 格门 + 玻璃窗 + 室内灯）、1..5 格尺度梯；5 个机位。
   - `BuiltScene.props` 由运行器走真实资产管线生成；`scene.props` 写入 manifest，`MANIFEST_SCHEMA_VERSION` 1→2（附加字段，默认场景仍为 S01/S02/S06，`DEFAULT_RUN` 保持 A01 基线稳定）。
   - manifest 的 `visual_style_version` 改为读 `art::ART_STYLE_VERSION`，避免两套版本号。
5. `art/style-guide.md`：形态/构图、色板与发光分级、材质家族指针、manifest schema 与审计用法、校准架判读、修复顺序。
6. 真实功能修订：`CREDITS.md` 删除不存在的 `vendor/bevy-volumetric-clouds` 条目（仓库无 vendor；云为自研 `cloud_shell.wgsl` + `CloudShellMaterial`）；README 增加 `--art-audit`、B01 场景与 `art/style-guide.md` 入口。

## 实测验收

```powershell
cargo fmt --all -- --check
cargo check --locked --all-targets --all-features
cargo clippy --locked --all-targets --all-features -- -D warnings
cargo test --locked --all-targets --all-features          # 286 passed / 0 failed（B01 新增 12）
cargo build --locked
target\debug\starforge-bevy.exe --art-audit
target\debug\starforge-bevy.exe --visual-qa --visual-qa-scene B01 --visual-qa-run b01-review2
```

- 审计（源码树）：`assets=39 live=18 reserve=11 optional=10/10 missing_required=0 case_mismatch=0 license_gaps=0 unreferenced=0`，`ART_AUDIT_OK`，exit 0；报告 `target/art-audit/art-manifest.json`。
- 缺包审计（`STARFORGE_ASSET_DIR` 指向只含自带模型/着色器的隔离根）：`source_tree=false optional=0/10 missing_required=0 license_gaps=26`；证明缺下载包不阻断，同时暴露“最小包未带许可文本”给 K04/B06。
- 校准架：`VISUAL_QA_OK scenes=1`；`target/visual-qa/c6f0c1d/B01/b01-review2/raw-frames/{overview,terrain,machines,characters,building}_*.png`。首轮发现 building 机位过近，已改为后拉抬高并复跑；四类样板同灯、同曝光、同分辨率可比。
- 故障注入/回归：临时目录 fixture 检出“缺失 optional/未引用文件”；Windows 大小写测试检出 `models/Creatures` 错误；单测发现并修复真实许可文件名漂移（`quaternius_ultimate_animated_animals_LICENSE.txt`）；key 冲突测试发现并修复 `scene.gltf` 命名冲突。

## 兼容、回滚与故障注入

- 存档/协议/依赖：零变化；`Cargo.toml`/锁文件未动。
- Manifest：schema 1→2 为附加 `scene.props`，旧消费者可忽略；默认 `--visual-qa` 场景集不变，A01/A02 场景输出除新增字段外保持同构。
- 回滚：删除 `src/art/`、`art/`、`SceneId::B01` 与运行器 props 接线即可；无数据迁移。
- 用户存档：`saves/` 三个文件时间戳保持 02:06:51，未写入。

## 未完成 / 未验证

1. 四类样板目前是“可同灯对照的校准架”，不是人工 1–5 分评审；配对审图归 K05。
2. 画面仍含实时玩法元素（兽群/HUD/粒子）；A01 已记录，冻结玩法开关不在本包。
3. 图标、方块纹理、机器/角色/建筑新模型均未制作；B01 只冻结规范、色板、尺度与 manifest，B02–B05 按此生产。
4. KayKit（CC0）与 Poly Pizza（CC-BY-3.0）的许可证原文不在 `assets/licenses/`：KayKit/Legacy 条目以 `CREDITS.md` 为许可登记；K04 打包必须附带 `CREDITS.md`。CC-BY-3.0 遗留生物若最终不入包，应在 B06 改为删除条目。
5. 审计只扫描 AssetServer 加载的 `models/`+`shaders/`；音频/字体为 `include_bytes!` 内嵌，由 CREDITS/打包检查覆盖。
6. `--art-audit` 报告固定写 `target/art-audit/`，未纳入 K01 报告 schema（未带 commit/run 元数据）；K01 可复用 `ArtAuditReport` 序列化。
7. 未在 Linux/打包发布目录跑大小写审计（逻辑有 Windows 单测；真实 Linux 运行归 K04/S23）。

## Rust 净增（物理行）

| 类别 | 新增 | 说明 |
| --- | ---: | --- |
| `src/art/mod.rs` | 63 | 根解析 + 2 测试 |
| `src/art/manifest.rs` | 677 | 清单/审计/CLI，其中测试约 130 |
| `src/art/style.rs` | 184 | 色板/发光/尺度，其中测试约 39 |
| `visual_qa/scene.rs`（B01 部分） | 约 211 | 校准架 + props + 测试 |
| `visual_qa/mod.rs`（B01 部分） | 约 21 | props 生成、schema v2、style 版本 |
| `char.rs` | 14 | `spawn_humanoid_model` 抽取 |
| `app.rs` / `main.rs` | 约 16 | `--art-audit` 接入 |
| **合计** | **约 1,186** | 其中测试约 215 |

预算 500，超 +137%。说明（05 的 5.13）：39 条带来源/许可/角色的清单本身是数据内容，审计的“缺失/大小写/许可/未引用”四类检查与 B01 视觉校准架是验收证据，没有占位或重复模板；可削减项只有 reserve 条目（但正是 B06 决策依据）。建议评审时把 B01 拆为 B01（规范+manifest）与 B01b（校准架）重排流 B 预算，或接受该超额并同步上调 B 流占比。

## 下一包建议

B02 可直接消费 `ArtAsset.key/path/source/license` 作为材质/视觉 ID 与图标清单的来源，并用 `--art-audit` 做“新增资产先登记”的门禁；B04 材料庭院应复用 B01 校准架的机位/曝光约定，不要另建第二套色板。B06 需处理：reserve 船接线或删除、Poly Pizza 条目去留、最小包许可文件打包、Linux 大小写审计。
