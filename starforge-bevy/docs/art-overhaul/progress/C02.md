# 工作包：C02

状态：active
负责人：codex
开始基线：`84991bfa5081a81bb9e2ba1af55170022f7d7f62`
依赖：C01（MeshBuilder/网格 hash/地形诊断）、B02（稳定 `SurfaceMaterialId`），均为 review；同时消费 B04 的 normal/ORM 工件。
范围：05 的 5.4 C02、03 的 3.1、06 的 R025–R027/R040。

## 已完成切片

1. **六类渲染分桶**：`ChunkMeshBuilder` 输出 opaque/cutout/transparent/emissive/special/liquid；区块分别持有实体，流式更新和卸载保持原子替换。不透明材质使用背面剔除，cutout/transparent 保留双面语义；同 ID 透明块内部面不再生成。
2. **顶点契约**：每个顶点现有 position/normal/UV/tangent/AO/material ID/color；AO 与用于旧视觉的顶点色分开，材质 ID 直接来自 B02，签名覆盖全部流。
3. **B04 normal 接线**：世界 solid/cutout/transparent/emissive/special 材质绑定线性 normal atlas；ORM 继续与 albedo 同 UV。HDR emission 未注入，emissive 分桶仍沿用原顶点亮度，强度/Bloom 协调留给 D04/H04。
4. **绕序/法线修复**：旧双面材质遮蔽了各面绕序不一致。发射器现根据几何法线与声明法线自动选索引方向，交叉植被也使用真实对角面法线。
5. **8 邻域 halo/AO**：网格构建前要求八个水平邻块都已生成；角点编辑会同时脏化对角区块，新区块物化也会脏化八邻域，解决 AO 跨角亮缝。
6. **诊断**：`TerrainDiagnostics` schema 升为 2，near 单独计数五个非液体分桶和 water；完整性/驻留/卸载判定已不再假设每块必有 opaque/water。

## 当前回归基线

- lush seed 11 chunk(0,0) combined solid：`0xbb38a232d823adb3`，6340 顶点。减少 236 顶点来自同 ID 叶片内面和完整机器邻面的正确裁剪。
- ocean seed 11 chunk(0,0) water：`0xee786d53d2a687cd`，1248 顶点（几何数不变，签名变化来自 tangent/AO/material ID 与正确绕序）。
- 新增单测覆盖：六分桶分类、透明内面、全顶点流对齐/单位法线/切线正交/有效材质 ID/AO 范围、六面绕序、角点对角脏化。

## 视觉检查

```powershell
cargo fmt --all -- --check
cargo clippy --locked --all-targets --all-features -- -D warnings
cargo test --locked --all-targets --all-features
cargo build --locked
target\debug\starforge-bevy.exe --visual-qa --visual-qa-scene S01,S06 --visual-qa-run c02-winding --terrain-overlay
```

- 格式检查和严格 Clippy 通过；全量单测 `381 passed / 0 failed`。
- `VISUAL_QA_OK scenes=2`；证据在 `target/visual-qa/84991bf/{S01,S06}/c02-winding/`（target 不入库）。
- 首轮 `c02-active` 因启用背面剔除暴露旧绕序错误，路线截图有大面积缺面；修复后 `c02-winding` 同机位无该缺陷，作为有效验收证据。
- S01 near：620 opaque / 556 cutout / 1 transparent / 2 emissive / 3 special / 89 water，`missing_view_chunks=0`。
- S06 near：589 opaque / 533 cutout / 5 transparent / 1 emissive / 0 special / 99 water，`missing_view_chunks=0`。

## 兼容与性能影响

- 存档块 ID、RLE、网络协议、物理/射线逻辑均未变。
- 分桶使每区块的 GPU mesh 实体数增加，但只为实际非空分桶创建；C03 必须把这些实体/上传统一纳入帧预算。
- opaque 恢复背面剔除；cutout/transparent 双面。normal atlas 增加一张 256×256 RGBA8 线性纹理。

## 未完成 / 下一切片

1. **受约束贪婪合面未完成**：当前发布材质仍直接采样 classic atlas；把合并 quad 的 UV 扩展到 1 格以外会读到其他 atlas cell，拉伸单格则触发 R026。下一切片应先把 B03 texture-array/padded-atlas 路由真正绑定到地形 shader，再只合并 material/direction/variant/AO/light/transparency 完全相容的面。
2. 未进行实机各向异性和全 mip 链的掠射角检查；当前世界材质仍是 Nearest classic atlas，本切片没有伪称其已达标。
3. 透明块保留材质双面以提供反面，但尚未做区块内透明 quad 的距离排序；F03/E05 需继续验证水/天气组合。
4. C03 的异步 revision、时间预算和 GPU 上传预算未开始。
